const Header = () => {

    return (
        <div>
            <nav className="navbar theme navbar-dark"> 
                    <a className="navbar-brand" href="#">Admin UI</a>
            </nav>
        </div>
    )

}

export default Header;